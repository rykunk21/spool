```rust
let x = [0.0, 1.0, 2.0, 3.0];
let y = [0.0, 1.0, 4.0, 9.0];
plot(x,y,"Quadratic", "line");

```

```rust
let df = read_csv("text.csv");
let col_a = df.select("col_a");
let col_b = df.select("col_b");
plot(col_a, col_b, "A vs B", "heatmap");






```

